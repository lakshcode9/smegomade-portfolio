import './CustomCursor.css';

// We now rely entirely on the native CSS cursor rule defined in CustomCursor.css 
// using the hand-drawn image rather than GSAP divs for maximum performance/zero-latency.
export default function CustomCursor() {
  return null;
}
